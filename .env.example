NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# URL of the separately deployed backend API server.
# Leave empty to use the built-in Next.js API routes (default for local dev).
NEXT_PUBLIC_API_URL=
